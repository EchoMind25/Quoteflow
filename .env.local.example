# =============================================================================
# Quotestream — Environment Variables
# =============================================================================
#
# SECURITY: Never commit .env.local to git. This file is already in .gitignore.
# Copy this file to .env.local and fill in your real values.
#
#   cp .env.local.example .env.local
#
# =============================================================================

# --- Supabase -----------------------------------------------------------------
# Project URL and keys from https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://abcdefghijkl.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.example-anon-key
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.example-service-role-key

# --- Anthropic (Claude AI) ---------------------------------------------------
# API key from https://console.anthropic.com/settings/keys
# Used for AI-powered quote generation (vision + text analysis)
ANTHROPIC_API_KEY=sk-ant-api03-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# --- Google Gemini ------------------------------------------------------------
# API key from https://aistudio.google.com/apikey
# Used as fallback AI provider
GOOGLE_AI_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# --- AssemblyAI ---------------------------------------------------------------
# API key from https://www.assemblyai.com/app/account
# Used for voice note transcription
ASSEMBLYAI_API_KEY=abcdef1234567890abcdef1234567890

# --- Email (SMTP via Resend) --------------------------------------------------
# Configure Resend as SMTP relay. See https://resend.com/docs/send-with-smtp
# Also configure these same credentials in Supabase Dashboard > Auth > SMTP Settings
# for auth emails (magic links, confirmations).
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_USER=resend
SMTP_PASS=re_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
SMTP_FROM=Quotestream <noreply@quotestream.app>

# --- Twilio (SMS) -------------------------------------------------------------
# Credentials from https://console.twilio.com
# Used for sending quote SMS notifications
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_PHONE_NUMBER=+15551234567

# --- Stripe (Payments) -------------------------------------------------------
# Keys from https://dashboard.stripe.com/apikeys
# Used for subscription billing
STRIPE_SECRET_KEY=sk_test_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
STRIPE_WEBHOOK_SECRET=whsec_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# --- Upstash Redis ------------------------------------------------------------
# Credentials from https://console.upstash.com
# Used for API rate limiting
UPSTASH_REDIS_REST_URL=https://example-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=AXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXx

# --- Encryption ---------------------------------------------------------------
# 32-byte hex string (64 characters) for AES-256-GCM encryption
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# --- App Config ---------------------------------------------------------------
# Public-facing URL (used for public quote links, email callbacks, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Node environment — set automatically by Vercel, override locally if needed
NODE_ENV=development
